# Starlette（FastAPI 依赖）要求 anyio<5；显式约束避免与 uvicorn/celery 等传递依赖解析到 anyio 5+
anyio>=3.4.0,<5

fastapi==0.115.6
uvicorn[standard]==0.32.1
celery[redis]==5.4.0
redis==5.2.1
sqlalchemy==2.0.36
alembic==1.14.0
pydantic-settings==2.6.1
python-multipart==0.0.20
websockets==14.1
